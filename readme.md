# Installation


move to `fastapi` dir and run this command `uv pip install -e . --index-strategy unsafe-best-match`


## Helpful command 
list the services at specific port 

`ss -tlnp | grep :8000`

kill the process 
`sudo kill (pid)`

